apiVersion: v1
kind: Service
metadata:
  name: zabbix-server
  labels:
    app: zabbix
    tier: server
spec:
  ports:
    - port: 10051
      targetPort: 10051
      name: zabbix-server
      protocol: TCP
  externalIPs:
    - {{ .Values.externalIP }}
  selector:
    name: zabbix-server 