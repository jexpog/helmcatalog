apiVersion: v1
kind: Service
metadata:
  name: zabbix-server
  labels:
    app: zabbix
    tier: server
spec:
  ports:
    - port: 10050
      targetPort: 10050
      name: zabbix-server
      protocol: TCP
  externalIPs:
    - {{ .Values.externalIP }}
  selector:
    name: zabbix-server 
