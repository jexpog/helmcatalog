apiVersion: v1
kind: Service
metadata:
  name: {{ include "zabbix.fullname" . }}-zabbix-server
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  ports:
  - port: 10051
    targetPort: 10051
    name: {{ include "zabbix.fullname" . }}-zabbix-trapper
  - port: 162
    targetPort: 162
    protocol: UDP
    name: {{ include "zabbix.fullname" . }}-snmp-trap
  selector:
    name: {{ include "zabbix.fullname" . }}-zabbix-server
  externalIPs:
  - "{{ .Values.ExternalIP }}"