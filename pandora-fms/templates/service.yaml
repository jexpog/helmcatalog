apiVersion: v1
kind: Service
metadata:
  name: {{ include "pandora-fms.fullname" . }}
  labels:
    {{- include "pandora-fms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pandora-fms.selectorLabels" . | nindent 4 }}
