apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pandora-fms.fullname" . }}-test-connection"
  labels:
{{ include "pandora-fms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pandora-fms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
