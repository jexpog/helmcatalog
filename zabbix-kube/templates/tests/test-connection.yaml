apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zabbix-kube.fullname" . }}-test-connection"
  labels:
{{ include "zabbix-kube.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "zabbix-kube.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
