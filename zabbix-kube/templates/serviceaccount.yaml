{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zabbix-kube.serviceAccountName" . }}
  labels:
{{ include "zabbix-kube.labels" . | nindent 4 }}
{{- end -}}
