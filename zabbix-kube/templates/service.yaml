apiVersion: v1
kind: Service
metadata:
  name: {{ include "zabbix-kube.fullname" . }}
  labels:
    {{- include "zabbix-kube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zabbix-kube.selectorLabels" . | nindent 4 }}
